# Contributing

## Before opening a PR

- Keep secrets out of Git.
- Run backend tests with `pytest -v`.
- Run frontend type checks with `npm run lint`.
- Keep changes focused and documented.
- Update the README when user-facing architecture or setup changes.
